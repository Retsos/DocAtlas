services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend/DocAtlas
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped

  chatbotclient:
    build:
      context: ./frontend/chatbotClient
    ports:
      - "5174:5174"
    env_file:
      - ./frontend/chatbotClient/.env
    restart: unless-stopped
